#!/bin/bash

# This is a script that configures git whenever we start working
# on multiple machines. Run it anytime you decide to work with
# git on a new computer. 
git config --global user.name "John Knowles"
git config --global user.email "joknowle@calpoly.edu"
git config --global color.ui "auto"
git config --global core.editor "nano"

